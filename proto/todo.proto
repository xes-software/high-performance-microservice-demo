syntax = "proto3";
package todo;

message AddRequest {
    string item = 1;
}

message ListResponse {
    repeated string list = 1;
}

service Todo {
    rpc Add(AddRequest) returns (ListResponse);
    rpc List() returns (ListResponse);
}
